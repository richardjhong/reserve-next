query ValidUser {
  validUser {
    id
    first_name
    last_name
    email
    city
  }
}

query CheckAvailability($input: AvailabilityInput!) {
  availability(input: $input) {
    availabilities {
      time
      available
    }
  }
}