query ValidUser {
  validUser {
    id
    first_name
    last_name
    email
    city
  }
}

query CheckAvailability($input: AvailabilitiesInput!) {
  availabilities(input: $input) {
    time
    available
  }
}